// import { AkirMarkEditor } from '@/components';

export const Route = createLazyFileRoute('/')({
  component: () => {
    return (
      <main>
        {/* <AkirMarkEditor /> */}
      </main>
    );
  },
});
